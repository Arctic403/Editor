LOCAL BRIDGE RETIRED
====================

This editor now uses the Cloudflare Worker route so it works from iPhone without a PC or localhost server.

Use AI_WORKER_SETUP.md and the cloudflare-ai-worker/ folder.

The old codex-backend/ folder is no longer required and has been removed from this patched build.
